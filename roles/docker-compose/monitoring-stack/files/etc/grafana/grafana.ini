[auth.anonymous]
# enable anonymous access
enabled = true

# specify organization name that should be used for unauthenticated users
org_name = Main Org.

# specify role for unauthenticated users
org_role = Viewer

# hide user's profile/dashboard settings options (only affects unauthenticated users)
hide_version = false
